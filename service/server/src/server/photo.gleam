import server/context
import wisp

pub fn get(request: wisp.Request, context: context.Context, photo: String) -> a {
  todo
}

pub fn upload(request: wisp.Request, context: context.Context) -> wisp.Response {
  todo
}
