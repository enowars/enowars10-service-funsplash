import server/web
import wisp

pub fn get(
  request: wisp.Request,
  context: web.Context,
  collection: String,
) -> wisp.Response {
  todo
}

pub fn create(
  request: wisp.Request,
  context: web.Context,
  collection: String,
) -> wisp.Response {
  todo
}
