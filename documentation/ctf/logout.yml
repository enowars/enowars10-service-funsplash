info:
  name: logout
  type: http
  seq: 17

http:
  method: POST
  url: http://127.0.0.1:1337/napi/logout
  headers:
    - name: content-type
      value: multipart/form-data
    - name: Content-Type
      value: multipart/form-data
  body:
    type: multipart-form
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
