info:
  name: photos
  type: http
  seq: 8

http:
  method: POST
  url: http://localhost:1337/upload
  headers:
    - name: content-type
      value: multipart/form-data
    - name: Content-Type
      value: multipart/form-data
  body:
    type: multipart-form
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
