[project]
name = "checker3"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"

dependencies = [
  "enochecker3==0.14.0",
  "uvicorn",
  "gunicorn",
  "faker",
  "qrcode",
  "pillow",
  "pyzbar",
]
